# Exel to Text Convertor

This is windows application that can generate .txt files per row in target .xlsx file

- Build the project
```
dotnet build
```

- Create .exe application
```
dotnet publish -c Release -r win-x64 --self-contained -o ./publish
```